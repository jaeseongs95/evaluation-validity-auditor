interface:
  display_name: "Evaluation Validity Auditor"
  short_description: "평가 설계와 결과의 타당성을 독립적으로 감사합니다"
  default_prompt: "Use $evaluation-validity-auditor to audit this evaluation before and after execution."
policy:
  allow_implicit_invocation: true
